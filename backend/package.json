{
  "name": "backend",
  "version": "1.0.0",
  "description": "API para dados financeiros",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Skip build step for backend' && exit 0",
    "dev": "nodemon server.js",
    "render-setup": "npm install && cd ../frontend && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "axios": "^1.6.2",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}